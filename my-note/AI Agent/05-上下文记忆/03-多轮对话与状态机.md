# 多轮对话与状态机

> 多轮 Agent 需要把消息、任务状态、下一步节点和中断信息持久化，使执行能够分支、暂停、恢复、回滚和等待人工决策。

## 为什么不只保存聊天记录

复杂 Agent 的状态不只有 messages，还可能包含：

- 当前目标和计划
- 已完成、待执行和失败步骤
- 工具调用及结果
- Token 与费用预算
- 用户、项目和权限作用域
- 待审批动作与恢复位置

只保存聊天文本无法可靠恢复这些执行状态。

## 图与状态机

课表提出使用 Graph 管理复杂状态流转。可以把节点理解为模型调用、工具执行、校验或人工审批，把边理解为固定或条件转移。

```text
接收请求 → 模型决策 → 是否调用工具？
                       ├─ 是 → 工具执行 → 回到模型决策
                       └─ 否 → 验收 → 输出
```

循环必须有终止条件，条件分支必须有无法匹配时的兜底路径。

## Checkpoint 与线程

检查点保存某一线程在特定时刻的完整状态。恢复执行时必须定位到同一个线程和检查点，否则系统不知道从哪个状态继续。

Checkpoint 支持：

- 多轮对话连续性
- 失败后的重试或回滚
- 长任务暂停后继续
- Human-in-the-Loop 审批
- 调试时查看执行停在哪里

## Human-in-the-Loop

Deep Agents 链接将人工决策区分为：

| 决策 | 含义 |
| --- | --- |
| approve | 按原参数执行 |
| edit | 修改参数后执行 |
| reject | 不执行，并把原因反馈给 Agent |
| respond | 人类直接提供工具结果，适合询问用户类工具 |

删除、发送、部署等副作用工具应使用 approve/edit/reject；`respond` 不应被当作软拒绝，因为模型可能把它理解为工具已经成功完成。

## 中断与恢复流程

1. Agent 运行到敏感动作。
2. Runtime 保存线程状态并返回中断信息。
3. 用户查看工具名称、参数和影响范围。
4. 用户批准、修改或拒绝。
5. 使用相同线程标识恢复执行。

没有持久化 Checkpointer，中断后就缺少可靠恢复基础。

## 风险分层

- 低风险只读操作通常自动执行。
- 写入和外部 API 可按场景审批。
- 删除、发布、发信、生产 SQL 等高风险操作必须严格审批。
- 即便是只读操作，访问密钥和隐私路径也不能自动放行。

## 关联笔记

- [[01-上下文工程]]
- [[02-记忆架构]]
- [[../04-工具与扩展/04-沙箱与执行安全|沙箱与执行安全]]

## 来源与核对范围

- [POPO：Agent开发学习课表](https://docs.popo.netease.com/team/pc/EHRZHAOPIN/pageDetail/5b5253077e2a43f7ac89deedfc6db29a)
- [AI Agent Book：多轮对话设计](https://waylandz.com/ai-agent-book/%E7%AC%AC09%E7%AB%A0-%E5%A4%9A%E8%BD%AE%E5%AF%B9%E8%AF%9D%E8%AE%BE%E8%AE%A1/)
- [Deep Agents：Human-in-the-Loop](https://datawhalechina.github.io/deepagents-in-action/chapters/ch09-human-in-the-loop/)
- [Hello-Agents：LangGraph 框架实践](https://datawhalechina.github.io/hello-agents/#/./chapter6/%E7%AC%AC%E5%85%AD%E7%AB%A0%20%E6%A1%86%E6%9E%B6%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5?id=_65-%e6%a1%86%e6%9e%b6%e5%9b%9b%ef%bc%9alanggraph)

> 本文描述的是状态机与 HITL 的稳定概念；具体 API 字段和版本要求应以对应框架的当前文档为准。
